/*using TMPro;
using UnityEngine;

public class HealthUI : MonoBehaviour
{
    private CharacterHealth _health;
    [SerializeField] private TextMeshProUGUI _healthValue;

    public void Start()
    {
        _health = GameManager.Instance.Player.Health;
        //_health.GotHit += OnHit;
        _healthValue.text = _health.CurrentHealth.ToString();
    }

    public void Update()
    {
        _healthValue.text = _health.CurrentHealth.ToString();
    }
}*/
